# genomas

## Aplica migraciones a la BD nueva (usando DATABASE_URL)
npx prisma migrate deploy

## Verifica estado de migraciones
npx prisma migrate status

## Regenera cliente Prisma (opcional)
npx prisma generate
